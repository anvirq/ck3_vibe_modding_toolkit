name: feedback_profile_c_ast256
description: "Profile C (mod-only) + AST chunking 256."

data:
  roots:
    mod:
      - common
      - events
      - gui
      - localization
  wiki_files: []

chunking:
  strategy: ast
  chunk_size: 256
  chunk_overlap: 0

retrieval:
  alpha: 0.5
