name: feedback_profile_c
description: "Profile C — mod-only (data/mod/**). Empty until you add a mod mirror under data/mod/."

data:
  roots:
    mod:
      - common
      - events
      - gui
      - localization
  wiki_files: []

chunking:
  strategy: sentence_splitter
  chunk_size: 256
  chunk_overlap: 50

retrieval:
  alpha: 0.5
