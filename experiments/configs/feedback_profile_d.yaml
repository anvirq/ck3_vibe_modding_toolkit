name: feedback_profile_d
description: "Profile D — hybrid minimal production: mod/** + narrow vanilla (no full wiki)."

data:
  roots:
    vanilla:
      - common/on_action
      - events/travel_events
      - common/scripted_guis
      - gui/window_army_select_commander.gui
    mod:
      - common
      - events
      - gui
      - localization
  wiki_files: []

chunking:
  strategy: sentence_splitter
  chunk_size: 256
  chunk_overlap: 50

retrieval:
  alpha: 0.5
