name: feedback_profile_a
description: "Profile A — vanilla core scripting (on_action, travel_events, scripted_guis, one GUI)."

data:
  roots:
    vanilla:
      - common/on_action
      - events/travel_events
      - common/scripted_guis
      - gui/window_army_select_commander.gui
  wiki_files: []

chunking:
  strategy: sentence_splitter
  chunk_size: 256
  chunk_overlap: 50

retrieval:
  alpha: 0.5
