name: feedback_profile_b
description: "Profile B — Profile A + full wiki."

data:
  roots:
    vanilla:
      - common/on_action
      - events/travel_events
      - common/scripted_guis
      - gui/window_army_select_commander.gui
  wiki_files: all

chunking:
  strategy: sentence_splitter
  chunk_size: 256
  chunk_overlap: 50

retrieval:
  alpha: 0.5
