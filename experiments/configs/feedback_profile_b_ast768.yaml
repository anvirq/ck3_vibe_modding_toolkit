name: feedback_profile_b_ast768
description: "Profile B + AST chunking 768."

data:
  roots:
    vanilla:
      - common/on_action
      - events/travel_events
      - common/scripted_guis
      - gui/window_army_select_commander.gui
  wiki_files: all

chunking:
  strategy: ast
  chunk_size: 768
  chunk_overlap: 0

retrieval:
  alpha: 0.5
